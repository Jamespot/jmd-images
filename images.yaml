# Bibliothèque d'images Jamespot pour JMD — l'index.
# Une entrée par image, le contrat est dans README.md, l'ajout se fait par le skill add-images.
# `npm run check` vérifie ce fichier et les dossiers.
[]
